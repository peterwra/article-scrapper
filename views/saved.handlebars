<div class="container">
    <div class="row">
        <div class="col-md-8">
            <div id="articles">
                {{#if articles}}
                    {{#each articles}}
                        {{#if isSaved}}
                            <hr>
                            <div class="card">
                                <div class="card-body">
                                    <h5 class="card-title">{{this.title}}</h5>
                                    <p class="card-text">{{this.link}}</p>
                                    <div class="btn-group" role="group">
                                        <button class="btn btn-secondary notes" data-id="{{this._id}}">Add/View Notes</button>
                                        <button class="btn btn-danger delete-article" data-id="{{this._id}}">Delete Article</button>
                                    </div>
                                </div>
                            </div>
                        {{/if}}
                    {{/each}}
                {{/if}}
            </div>
        </div>
        <div class="col-md-4">
            <div id="notes"></div>
        </div>
    </div>
</div>
